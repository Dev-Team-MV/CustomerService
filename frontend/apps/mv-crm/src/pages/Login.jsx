import React from 'react'
// TODO: Implementar login del CRM (solo admins)
// Usar authService de @shared/services/authService
export default function Login() {
  return <div>MV CRM - Login (admin only)</div>
}
