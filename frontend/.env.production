# VITE_API_URL lo fija Jenkins en el build (Frontend-Production → apics, Frontend-Develop → apicsdev)
# No definir aquí para no pisar el --build-arg del pipeline.
# VITE_API_URL=https://apics.michelangelodelvalle.com/api